---
title: "📚 [React 기초 3-4/8] State 보존 및 재설정"
date: "2025-07-26"
description: "React에서 State가 보존되고 재설정되는 규칙을 이해하고 key를 활용하는 방법을 학습합니다"
tags: ["React", "학습", "Vue", "초급", "프론트엔드"]
series: "React 기초"
seriesOrder: 22
---

# React 학습 가이드 - State 보존 및 재설정

> React 공식 문서를 기반으로 한 학습 자료  
> 원문: [Managing State - React](https://react.dev/learn/managing-state)

---

## State 보존 및 재설정

React는 언제 state를 유지하고 언제 초기화할까요? 이를 이해하면 버그를 피할 수 있습니다.

### React는 트리 구조에서 state를 보존합니다

```jsx
import { useState } from 'react';

export default function App() {
  const [showB, setShowB] = useState(true);
  
  return (
    <div>
      <Counter />
      {showB && <Counter />}
      <button onClick={() => setShowB(!showB)}>
        {showB ? '두 번째 Counter 숨기기' : '두 번째 Counter 보이기'}
      </button>
    </div>
  );
}

function Counter() {
  const [count, setCount] = useState(0);
  return (
    <div>
      <button onClick={() => setCount(count + 1)}>
        Count: {count}
      </button>
    </div>
  );
}
```

**동작:**

- 두 `<Counter />` 모두 **독립적인 state** 보유
- 첫 번째 Counter와 두 번째 Counter는 **별도로 카운트**됨
- `showB`를 false로 변경 → 두 번째 Counter의 state **사라짐** (컴포넌트가 DOM에서 제거됨)
- `showB`를 다시 true로 → 두 번째 Counter는 **0부터 시작** (새로운 인스턴스 생성)

### 같은 위치의 같은 컴포넌트는 state를 보존합니다

React는 컴포넌트의 UI 트리 상 위치로 state를 관리합니다.

```jsx
import { useState } from 'react';

export default function App() {
  const [isFancy, setIsFancy] = useState(false);
  
  return (
    <div>
      {isFancy ? (
        <Counter isFancy={true} />
      ) : (
        <Counter isFancy={false} />
      )}
      <label>
        <input
          type="checkbox"
          checked={isFancy}
          onChange={e => setIsFancy(e.target.checked)}
        />
        Use fancy styling
      </label>
    </div>
  );
}

function Counter({ isFancy }) {
  const [count, setCount] = useState(0);
  return (
    <div>
      <button
        onClick={() => setCount(count + 1)}
        style={isFancy ? {
          background: 'linear-gradient(45deg, #ff6b6b, #4ecdc4)',
          color: 'white',
          padding: '10px 20px',
          border: 'none',
          borderRadius: '5px'
        } : {}}
      >
        Count: {count}
      </button>
    </div>
  );
}
```

**결과:** `isFancy`를 변경해도 Counter의 state(`count`)는 **유지됨**!

**이유:**

- 같은 위치에 같은 컴포넌트 (`<Counter />`)
- React는 **동일한 컴포넌트**로 인식하여 state를 보존
- `isFancy` prop만 변경되고 컴포넌트는 재생성되지 않음

**테스트:**
1. 카운터를 몇 번 클릭하여 count를 증가시킴
2. 체크박스를 클릭하여 스타일 변경
3. **count 값이 유지되는지 확인** (0으로 리셋되지 않음)

### 다른 컴포넌트는 state를 재설정합니다

동일한 위치라도 컴포넌트 타입이 다르면 state가 초기화됩니다.

```jsx
import { useState } from 'react';

export default function App() {
  const [isPaused, setIsPaused] = useState(false);
  
  return (
    <div>
      {isPaused ? (
        <p>See you later!</p>
      ) : (
        <Counter />
      )}
      <label>
        <input
          type="checkbox"
          checked={isPaused}
          onChange={e => setIsPaused(e.target.checked)}
        />
        Take a break
      </label>
    </div>
  );
}

function Counter() {
  const [count, setCount] = useState(0);
  return (
    <div>
      <button onClick={() => setCount(count + 1)}>
        Count: {count}
      </button>
    </div>
  );
}
```

**결과:** `isPaused`를 변경하면 Counter의 state **재설정됨**!

**이유:**

- 다른 컴포넌트 (`<p>` vs `<Counter />`)
- React가 트리에서 **완전히 제거** 후 다시 추가
- 새로운 인스턴스가 생성되므로 state가 0으로 초기화됨

**테스트:**
1. 카운터를 몇 번 클릭하여 count를 증가시킴
2. 체크박스를 클릭하여 "Take a break" 활성화
3. 다시 체크박스를 클릭하여 Counter 표시
4. **count가 0으로 리셋되는지 확인** (state가 재설정됨)

### 같은 위치에서 state 재설정하기

같은 위치의 같은 컴포넌트인데 state를 초기화하고 싶다면 어떻게 할까요?

#### 방법 1: 다른 위치에 렌더링

같은 컴포넌트를 **JSX에서 다른 위치**에 렌더링하면 state가 재설정됩니다.

```jsx
import { useState } from 'react';

export default function Scoreboard() {
  const [isPlayerA, setIsPlayerA] = useState(true);
  
  return (
    <div>
      {isPlayerA && <Counter person="Taylor" />}
      {!isPlayerA && <Counter person="Sarah" />}
      <button onClick={() => setIsPlayerA(!isPlayerA)}>
        다음 플레이어!
      </button>
    </div>
  );
}

function Counter({ person }) {
  const [score, setScore] = useState(0);
  return (
    <div>
      <p>{person}: {score}</p>
      <button onClick={() => setScore(score + 1)}>+1</button>
    </div>
  );
}
```

**동작:**
- 두 개의 **독립적인 표현식**이므로 React는 다른 위치로 인식
- 플레이어를 전환하면 Counter가 **완전히 새로 생성**됨
- 각 플레이어는 **독립적인 state**를 가짐

**참고:** 삼항연산자(`? :`)를 사용하면 같은 위치로 인식되어 state가 보존됩니다:
```jsx
{isPlayerA ? <Counter person="Taylor" /> : <Counter person="Sarah" />}
```

#### 방법 2: key로 state 재설정 (권장)

같은 위치에 렌더링하더라도 **`key` prop을 변경**하면 state가 재설정됩니다.

```jsx
import { useState } from 'react';

export default function Scoreboard() {
  const [isPlayerA, setIsPlayerA] = useState(true);
  
  return (
    <div>
      {isPlayerA ? (
        <Counter key="Taylor" person="Taylor" />
      ) : (
        <Counter key="Sarah" person="Sarah" />
      )}
      <button onClick={() => setIsPlayerA(!isPlayerA)}>
        다음 플레이어!
      </button>
    </div>
  );
}

function Counter({ person }) {
  const [score, setScore] = useState(0);
  return (
    <div>
      <p>{person}: {score}</p>
      <button onClick={() => setScore(score + 1)}>+1</button>
    </div>
  );
}
```

**`key`를 사용하면:**

- 같은 위치더라도 다른 key = 다른 컴포넌트로 인식
- state 완전히 재설정됨
- **더 깔끔한 코드** (조건부 렌더링 없이 하나의 위치에 렌더링)

**비교:**
- **방법 1**: 물리적으로 다른 위치에 렌더링 (코드가 복잡해질 수 있음)
- **방법 2**: `key`로 강제 재설정 (권장, 더 간결함)

### 실전 예제: 채팅 앱

연락처를 바꿀 때마다 입력 필드를 초기화해야 하는 채팅 앱입니다.

```jsx
import { useState } from 'react';

export default function Messenger() {
  const [to, setTo] = useState(contacts[0]);
  
  return (
    <div>
      <ContactList
        contacts={contacts}
        selectedContact={to}
        onSelect={contact => setTo(contact)}
      />
      <Chat key={to.id} contact={to} />
    </div>
  );
}

function ContactList({ contacts, selectedContact, onSelect }) {
  return (
    <ul>
      {contacts.map(contact => (
        <li key={contact.id}>
          <button
            onClick={() => onSelect(contact)}
            style={{
              fontWeight: selectedContact.id === contact.id ? 'bold' : 'normal'
            }}
          >
            {contact.name}
          </button>
        </li>
      ))}
    </ul>
  );
}

function Chat({ contact }) {
  const [text, setText] = useState('');
  
  return (
    <div>
      <h3>채팅: {contact.name}</h3>
      <textarea
        value={text}
        onChange={e => setText(e.target.value)}
        placeholder={`${contact.name}에게 메시지를 입력하세요...`}
      />
      <button onClick={() => {
        alert(`메시지 전송: "${text}"`);
        setText('');
      }}>
        전송
      </button>
    </div>
  );
}

const contacts = [
  { id: 0, name: 'Taylor', email: 'taylor@mail.com' },
  { id: 1, name: 'Alice', email: 'alice@mail.com' },
  { id: 2, name: 'Bob', email: 'bob@mail.com' }
];
```

**`key={to.id}`를 사용하면:**

- 다른 연락처 선택 시 입력 필드(`text` state) **자동 초기화**됨
- 각 연락처마다 별도의 `Chat` 인스턴스 생성
- **`key` 없이 사용하면**: 연락처를 바꿔도 이전 메시지가 남아있음 (버그!)

**동작 확인:**
1. Taylor 선택 → 메시지 입력
2. Alice 선택 → **입력 필드가 비워짐** (state 재설정)
3. 다시 Taylor 선택 → **입력 필드가 비워짐** (새로운 인스턴스)

### Vue와 비교

Vue도 `key`를 사용하여 강제 재렌더링할 수 있습니다:

```html
<template>
  <Chat :key="selectedContact.id" :contact="selectedContact" />
</template>
```
