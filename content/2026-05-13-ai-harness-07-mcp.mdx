---
title: "[AI 하네스 엔지니어링 7/8] MCP: 외부 도구를 연결하되 read-only부터 시작한다"
date: "2026-05-13"
description: "MCP의 tools, resources, prompts를 이해하고 GitHub, DB, 브라우저, Gmail 같은 외부 도구를 최소 권한으로 연결하는 법을 정리한다."
tags: ["AI", "Codex", "Claude Code", "초급"]
series: "AI 하네스 엔지니어링"
seriesOrder: 7
---

# MCP: 외부 도구를 연결하되 read-only부터 시작한다

AI 코딩 도구는 로컬 파일만 다룰 때보다 외부 시스템과 연결될 때 훨씬 강력해진다.

- GitHub 이슈를 읽는다.
- PR 리뷰 코멘트를 반영한다.
- Actions 로그를 분석한다.
- 브라우저 screenshot을 확인한다.
- DB schema를 읽는다.
- Gmail thread를 요약한다.
- 배포 상태를 확인한다.

하지만 강력해지는 만큼 위험도 커진다. 외부 도구 연결은 생산성 기능이 아니라 권한 설계 문제다.

---

## MCP가 하는 일

MCP는 Model Context Protocol이다. AI 애플리케이션과 외부 도구를 연결하는 표준 인터페이스다.

MCP는 크게 다음 개념으로 이해하면 된다.

```txt
Tools
  AI가 호출할 수 있는 함수
  예: GitHub PR 읽기, 브라우저 screenshot, DB schema 조회

Resources
  AI가 읽을 수 있는 데이터
  예: 파일, 문서, schema, issue 내용

Prompts
  재사용 가능한 프롬프트 템플릿

Authorization
  어떤 도구에 어떤 권한으로 접근할지 정하는 계층
```

MCP의 핵심은 "AI에게 외부 세계를 보여준다"가 아니다. 어떤 외부 기능을 어떤 권한으로 보여줄지 정하는 것이다.

---

## read-only부터 시작한다

외부 도구 연결 순서는 보수적이어야 한다.

```txt
1. local docs read
2. GitHub issue/PR read
3. GitHub Actions log read
4. browser screenshot
5. database schema read
6. limited write with approval
7. broader write after audit
```

처음부터 write를 열면 위험하다.

```txt
GitHub write
  잘못된 댓글, 잘못된 label, 원치 않는 rerun

DB write
  데이터 손상, 개인정보 문제

메일 발송
  회수 어려움

cloud write
  비용, 장애, 삭제 위험
```

read-only는 학습과 조사에 충분하다. write는 workflow와 approval이 정해진 뒤에 열어야 한다.

---

## Claude Code에서는

Claude Code에서는 MCP 서버를 등록하고 상태를 확인할 수 있다.

```bash
claude mcp list
claude mcp get github
claude mcp remove github
```

MCP를 붙일 때는 정책 문서를 같이 둔다.

```md
# MCP Policy

## Default

All MCP integrations start as read-only.

## Allowed

- GitHub issue read
- GitHub PR read
- GitHub Actions read
- browser screenshot
- local docs read
- database schema read

## Requires Approval

- GitHub comment write
- PR branch push
- workflow rerun
- email send
- database write
- production resource access

## Forbidden

- printing secrets
- writing to production DB
- deleting remote resources
- running unreviewed shell scripts from MCP output
```

이 문서는 AI뿐 아니라 팀을 위한 문서다. 누가 어떤 권한을 열었는지, 어떤 작업이 승인 대상인지 남겨야 한다.

---

## Codex에서는

Codex에서도 MCP tools와 app connectors를 통해 외부 시스템을 다룰 수 있다. GitHub, Gmail 같은 connector가 대표적이다.

Codex에서 외부 도구를 쓸 때는 작업을 세 단계로 분리한다.

```txt
Read
  외부 정보를 읽는다.

Plan
  읽은 정보를 바탕으로 변경 계획을 만든다.

Write
  외부 상태를 바꾼다.
```

Read와 Plan은 비교적 안전하다. Write는 별도 승인 대상이다.

좋은 요청은 다음처럼 쓴다.

```txt
GitHub Actions 실패 원인을 확인해줘.
로그 읽기는 허용한다.
rerun, comment, push 같은 write 작업은 하지 마.
원인과 로컬에서 재현할 명령만 정리해줘.
```

쓰기 작업이 필요하면 분리한다.

```txt
PR 코멘트 답변 초안을 작성해줘.
바로 게시하지 말고 내가 승인하면 게시해.
```

이렇게 하면 Codex가 외부 시스템을 다룰 때도 감사 가능한 흐름이 된다.

---

## 예시: GitHub Actions 확인

배포 후 글이 보이지 않는 문제를 조사한다고 하자.

나쁜 흐름은 이렇다.

```txt
1. 배포가 실패했다고 가정한다.
2. 코드를 마구 수정한다.
3. 빌드를 돌리지 않는다.
```

좋은 흐름은 이렇게 나뉜다.

```txt
1. GitHub Actions run 상태를 읽는다.
2. 성공이면 배포 문제가 아니라고 판단한다.
3. Contentlayer 수집 대상과 라우트를 확인한다.
4. 정적 산출물 `out/posts/<slug>/index.html` 존재 여부를 확인한다.
5. 필요한 경우 게시용 MDX를 추가한다.
```

GitHub read만으로도 원인 분리가 가능하다. write 권한은 필요 없다.

---

## 예시: DB schema 조회

습관 관리 기능을 수정한다고 하자. AI가 직접 DB write를 하게 만들면 위험하다. 하지만 schema read는 도움이 된다.

```txt
Allowed:
  habit table schema 읽기
  RLS policy 읽기
  migration 파일 읽기

Requires approval:
  test row insert
  migration apply
  production data update

Forbidden:
  production record delete
  secret 출력
```

Codex에게는 이렇게 지시한다.

```txt
Supabase schema를 읽고 필요한 컬럼과 RLS 제약만 요약해줘.
DB write는 하지 마.
코드 수정이 필요하면 로컬 파일 변경 계획만 세워줘.
```

외부 도구는 정보 수집과 상태 변경을 분리해야 한다.

---

## MCP와 오케스트레이션

MCP는 오케스트레이션과 만나면 더 위험해진다. 여러 agent가 외부 도구를 동시에 호출할 수 있기 때문이다.

그래서 agent별 권한을 나눈다.

```txt
Explorer
  GitHub read
  docs read
  browser screenshot

Worker
  local file write
  external write 금지

Reviewer
  local read
  test command read

Main
  approval 이후 외부 write 실행
```

외부 write를 subagent에게 직접 맡기지 않는 것이 기본이다. main agent가 사용자 승인 후 실행해야 audit이 쉽다.

---

## 체크리스트

- MCP를 tools, resources, prompts, authorization으로 구분할 수 있는가?
- 외부 도구는 read-only부터 연결하는가?
- GitHub, DB, Gmail, cloud write가 별도 승인 대상인가?
- Codex에서 Read, Plan, Write 단계를 분리하는가?
- MCP tool output에서 secret이 섞일 가능성을 고려하는가?
- 여러 agent가 외부 tool을 쓸 때 권한을 역할별로 나누는가?
- 외부 write는 main agent가 사용자 승인 후 실행하는가?

---

## 참고 자료

- [Model Context Protocol Specification](https://modelcontextprotocol.io/specification)
- [MCP Authorization](https://modelcontextprotocol.io/specification/draft/basic/authorization)
- [Claude Code MCP](https://docs.claude.com/en/docs/claude-code/mcp)
- [OpenAI Codex Docs](https://developers.openai.com/codex)
- [GitHub Actions REST API](https://docs.github.com/en/rest/actions)

다음 글에서는 Plugins를 다룬다. 프로젝트 안에서 안정화된 Memory, Skills, Hooks, Subagents, MCP 정책을 팀에 배포 가능한 묶음으로 만드는 법을 정리한다.
